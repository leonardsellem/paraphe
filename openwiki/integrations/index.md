# Files

- [The ask and wait commands](ask-and-wait-cli.md) - The shell client any runner can background — how paraphe ask creates a card and prints its request id, how paraphe wait holds the card's lifetime in repeated bounded windows, and what each exit code, endpoint and credential resolution rule means.
- [Owner replies as answers](owner-reply-intake.md) - The second owner-side answer channel — how a private-chat long-press reply on a Telegram card message becomes that card's answer, from the adapter's owner and reply_to_message checks through resolution by replied-to message id, the claim ladder shared with a tap, the quiet-failure contract, the waiter wake and keyboard strip, and the envelope a reader gets.
- [The rendered card](telegram-card-rendering.md) - How the message the owner reads on the phone is composed — render_card's fixed section order and helpers, escaping and the UTF-16 budget with its deterministic trim, the decision and status shapes, the payload the inbox builds, and the invariants a change must preserve.
- [The Telegram tap surface](telegram-tap-surface.md) - The phone path end to end — the TelegramBotAPI client with its five methods (including get_me) and its two error kinds (NotifyRejected versus TelegramAPIError), callback data bound to the card version inside Telegram's 64-byte limit, the keyboard attach/strip and revision lifecycle across restarts, owner-only update filtering and the /config stub, and the long-polling loop with a durable offset.
