# Files

- [Card lifecycle](card-lifecycle.md) - The card record and every transition on it — idempotent create keyed on a caller external_id, the notification reservation written before the owner is told, the single claim ladder that all three owner-side doors enter, revision bumping and stale refusals, expiry, cancel and closeout — and why the SQLite store, never a destination, holds the answer.
- [Composition root and runtime](composition-root-and-runtime.md) - How one `paraphe` invocation becomes a running inbox — the entry point's dispatch of help, version, the check and store commands, the ask/wait client and the server, settings resolution from file and environment, the destination chosen from configuration, ordered startup with its rollbacks, the ready line, and the shutdown order.
- [The served MCP surface and envelope](mcp-surface.md) - How Paraphe produces and serves its twelve-tool MCP contract — the registry behind tool names, schemas and served text, the strictness rules that make the documented field set the whole field set, the provenance and choice_notes fields, the reading envelope with derived status, and the JSON-RPC and HTTP behaviour of /mcp and /answer.
- [The wait engine](wait-engine.md) - How a Paraphe tool call parks until the owner answers or its bounded window ends — per-card event registration under a dedicated lock, notification only after the durable write, the re-read that closes the registration race, every wake path (tap, owner reply, cancel, observed expiry), and why a disconnect or a restart leaves no residue.
